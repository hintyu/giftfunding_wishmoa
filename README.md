# 🎁 위시모아 (WishMoa)

> **선물펀딩프로젝트 위시모아** - 누구나 자신만의 선물 펀딩 페이지를 만들 수 있는 모바일 서비스

더 이상 치킨 기프티콘, 디퓨저는 그만! 이제 진짜 갖고 싶은 선물을 받아보세요.

---

## 📋 프로젝트 개요

### Before (v1.0)
- 특정 개인을 위한 선물 펀딩 사이트
- 프론트엔드 + Firebase Firestore
- Firebase Hosting 배포

### After (v2.0) ✨
- **다중 사용자** 플랫폼: 누구나 로그인하여 자신만의 펀딩 프로젝트 운영
- 프론트엔드 + 백엔드 + 관계형 데이터베이스
- **Vercel** 배포

---

## 🛠 기술 스택

| 구분 | 기술 / 라이브러리 | 역할 |
|------|------------------|------|
| **프레임워크** | Next.js 14 (App Router) | Vercel 최적화, SSR 기반 SEO 및 OG Tag 처리 |
| **언어** | TypeScript | DB 스키마와 프론트엔드 간 타입 안정성 |
| **스타일링** | Tailwind CSS | 빠른 UI 개발, 커스텀 폰트 적용 |
| **UI 컴포넌트** | shadcn/ui | Tailwind 기반 고품질 UI 컴포넌트 |
| **데이터 페칭** | TanStack Query | 서버 데이터 캐싱 및 상태 동기화 |
| **Drag & Drop** | dnd-kit | 선물 품목 순서 변경 |
| **백엔드 API** | Next.js API Routes | 서버리스 API 및 비즈니스 로직 |
| **데이터베이스** | Neon (PostgreSQL) | 관계형 DB, 확장성 및 데이터 무결성 |
| **ORM** | Prisma | 타입 안전한 DB 통신 |
| **인증** | NextAuth.js (Auth.js) | 네이버, 구글 소셜 로그인 |
| **파일 저장소** | Vercel Blob Storage | 이미지 업로드 및 저장 |
| **배포** | Vercel | Next.js 최적화 CI/CD |

---

## 📱 페이지 구성

### 1. 로그인 / 서비스 소개 페이지 (`/`)
- 서비스 소개 메인 페이지
- **네이버로 로그인하기** 버튼
- **Google로 로그인하기** 버튼
- 소셜 로그인만 지원 (이메일/비밀번호 없음)

### 2. 프로젝트 선택 페이지 (`/dashboard`)
- 로그인 후 진입하는 메인 대시보드
- **펀딩 프로젝트 시작하기** 버튼 → 신규 프로젝트 생성 페이지로 이동
- 기존 프로젝트 목록 표시 → 클릭 시 해당 펀딩 페이지로 이동
- **편집** 버튼으로 프로젝트 삭제 가능

### 3. 펀딩 신규 페이지 (`/dashboard/new`)
- 펀딩 타이틀 입력
- 부제목 입력
- **계좌 정보 입력** (은행명, 계좌번호, 예금주)
- 생성 완료 시 펀딩 프로젝트 페이지로 이동

### 4. 펀딩 프로젝트 페이지 (`/p/[project_id]`) ⭐
> 기존 v1.0 메인 페이지 소스 재활용

- **비로그인 사용자도 접근 가능** (공개 페이지)
- 선물 목록 확인
- 후원 버튼 클릭 → 후원 모달
- 계좌번호 복사 / 토스 딥링크

#### 프로젝트 소유자 전용 기능
우측 상단 **햄버거 메뉴** (본인에게만 표시):
- `선물 품목 관리` → 선물 품목 관리 페이지로 이동
- `후원 관리` → 후원 관리 페이지로 이동
- `프로젝트 관리` → 모달에서 title/subtitle/계좌정보 수정

### 5. 선물 품목 관리 페이지 (`/p/[project_id]/items`)
> 프로젝트 소유자만 접근 가능

- **선물 추가하기** 버튼 → 모달로 선물 추가
  - 제목
  - 마켓 URL
  - 목표 금액
  - 이미지 (URL 직접 입력 또는 파일 업로드)
- **드래그 앤 드롭**으로 표시 순서 변경
- 선물 상태 변경: `활성화` / `숨기기` / `완료` / `삭제`

### 6. 후원 관리 페이지 (`/p/[project_id]/donations`)
> 프로젝트 소유자만 접근 가능

- 해당 프로젝트의 모든 후원 내역 (최신순)
- 후원 상태 관리: `확인대기중` / `확인` / `삭제`
- 입금 확인 처리

---

## 🗄️ 데이터베이스 스키마

### 1. users (사용자)
> NextAuth.js가 자동 관리하는 테이블 포함

| 필드 | 타입 | 설명 |
|------|------|------|
| id | UUID (PK) | 사용자 고유 ID |
| name | string | 사용자 이름 |
| email | string | 이메일 |
| image | string | 프로필 이미지 URL |
| provider | string | 로그인 제공자 (naver/google) |
| created_at | timestamp | 가입일시 |

### 2. projects (펀딩 프로젝트)

| 필드 | 타입 | 설명 |
|------|------|------|
| project_id | string (PK) | 프로젝트 고유 ID (짧은 slug, 8자) |
| user_id | UUID (FK) | 소유자 ID (users 참조) |
| project_title | string | 프로젝트 제목 |
| project_subtitle | string | 프로젝트 부제목 |
| account_bank | string | 은행명 |
| account_number | string | 계좌번호 |
| account_holder | string | 예금주 |
| project_status | enum | 상태: `active` / `hidden` / `deleted` |
| created_at | timestamp | 생성일시 |
| updated_at | timestamp | 수정일시 |

### 3. items (선물 품목)

| 필드 | 타입 | 설명 |
|------|------|------|
| item_id | UUID (PK) | 선물 고유 ID |
| project_id | UUID (FK) | 프로젝트 ID (projects 참조) |
| item_title | string | 선물 제목 |
| item_url | string | 마켓 링크 URL |
| item_image | string | 이미지 URL |
| item_price | integer | 목표 금액 (원) |
| item_status | enum | 상태: `active` / `hidden` / `completed` / `deleted` |
| item_order | integer | 표시 순서 |
| created_at | timestamp | 생성일시 |
| updated_at | timestamp | 수정일시 |

### 4. donations (후원)

| 필드 | 타입 | 설명 |
|------|------|------|
| donation_id | UUID (PK) | 후원 고유 ID |
| item_id | UUID (FK) | 선물 ID (items 참조) |
| donator_nm | string | 후원자 이름 |
| donator_message | string | 후원 메시지 |
| donation_amount | integer | 후원 금액 (원) |
| donation_status | enum | 상태: `pending` / `confirmed` / `deleted` |
| created_at | timestamp | 후원일시 |

### ER 다이어그램
```
users (1) ──────< (N) projects
                        │
                        │ (1)
                        │
                        ▼
                      (N) items (1) ──────< (N) donations
```

---

## 🔐 권한 및 보안

### 페이지 접근 권한

| 페이지 | 비로그인 | 로그인 (타인) | 프로젝트 소유자 |
|--------|:--------:|:-------------:|:---------------:|
| 서비스 소개/로그인 | ✅ | ✅ | ✅ |
| 프로젝트 선택 | ❌ | ✅ (본인 것만) | ✅ |
| 펀딩 신규 | ❌ | ✅ | ✅ |
| 펀딩 프로젝트 | ✅ | ✅ | ✅ + 관리메뉴 |
| 선물 품목 관리 | ❌ | ❌ | ✅ |
| 후원 관리 | ❌ | ❌ | ✅ |

### API 권한 검증
- 모든 관리 API는 세션 검증 + 프로젝트 소유권 확인
- 후원 생성 API는 공개 (비로그인 허용)

---

## 📁 프로젝트 구조 (예상)

```
src/
├── app/
│   ├── page.tsx                    # 서비스 소개 + 로그인 페이지
│   ├── dashboard/
│   │   ├── page.tsx                # 프로젝트 선택 페이지
│   │   └── new/
│   │       └── page.tsx            # 펀딩 신규 페이지
│   ├── p/
│   │   └── [projectId]/
│   │       ├── page.tsx            # 펀딩 프로젝트 페이지 (공개)
│   │       ├── items/
│   │       │   └── page.tsx        # 선물 품목 관리
│   │       └── donations/
│   │           └── page.tsx        # 후원 관리
│   ├── api/
│   │   ├── auth/
│   │   │   └── [...nextauth]/
│   │   │       └── route.ts        # NextAuth.js
│   │   ├── projects/
│   │   │   └── route.ts            # 프로젝트 CRUD
│   │   ├── items/
│   │   │   └── route.ts            # 선물 CRUD
│   │   └── donations/
│   │       └── route.ts            # 후원 CRUD
│   └── layout.tsx
├── components/
│   ├── ui/                         # shadcn/ui 컴포넌트
│   ├── DonationModal.tsx           # 후원 모달 (기존 재활용)
│   ├── ItemCard.tsx                # 선물 카드 (기존 재활용)
│   └── ...
├── lib/
│   ├── prisma.ts                   # Prisma 클라이언트
│   ├── auth.ts                     # NextAuth 설정
│   └── constants.ts                # 상수 정의
├── hooks/
│   └── useProjects.ts              # TanStack Query 훅
├── types/
│   └── index.ts                    # 타입 정의
└── prisma/
    └── schema.prisma               # DB 스키마
```

---

## 🚀 시작하기

### 1. 저장소 클론
```bash
git clone https://github.com/hintyu/PLAIGROUND_wishmoa.git
cd PLAIGROUND_wishmoa
```

### 2. 의존성 설치
```bash
npm install
```

### 3. 환경변수 설정
`.env.local` 파일 생성:
```env
# Database (Neon)
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# NextAuth.js
# 로컬 개발: http://localhost:3000
# 프로덕션: https://wishmoa.vercel.app
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"

# Naver OAuth
NAVER_CLIENT_ID="xxx"
NAVER_CLIENT_SECRET="xxx"

# Google OAuth
GOOGLE_CLIENT_ID="xxx"
GOOGLE_CLIENT_SECRET="xxx"

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxxx"

# Gemini API (선물 추천 기능)
GEMINI_API_KEY="your-gemini-api-key"
```

### 4. 데이터베이스 마이그레이션
```bash
npx prisma migrate dev
npx prisma generate
```

### 5. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

---

## 📝 스크립트

| 명령어 | 설명 |
|--------|------|
| `npm run dev` | 개발 서버 실행 |
| `npm run build` | 프로덕션 빌드 |
| `npm run start` | 프로덕션 서버 실행 |
| `npm run lint` | ESLint 실행 |
| `npx prisma studio` | DB GUI 실행 |
| `npx prisma migrate dev` | DB 마이그레이션 |

---

## 🌐 배포

### Vercel 배포
1. Vercel에 GitHub 저장소 연결
2. 환경변수 설정
3. 자동 배포 완료

### 환경변수 (Vercel)
Vercel 대시보드에서 `.env.local`과 동일한 환경변수 설정 필요

---

## 🎨 디자인 시스템

### 컬러 팔레트
- **Primary Blue**: `#381DFC`
- **Primary Pink**: `#DE1761`
- **Light Pink**: `#E6A5BD`
- **Cyan**: `#65D5E8`
- **Yellow**: `#F3F33B`

### 폰트
- **Primary**: Omyu Pretty (커스텀 폰트)
- **Fallback**: system-ui

### 특징
- 모바일 퍼스트 디자인
- 그라데이션 헤더
- 컬러풀한 후원자 아이콘
- 부드러운 애니메이션

---

## 📄 라이센스

이 프로젝트는 개인 프로젝트입니다.

---

Made with 💙 by hintyu
