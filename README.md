# 🎁 위시모아 (WishMoa)

> **선물펀딩프로젝트 위시모아** - 누구나 자신만의 선물 펀딩 페이지를 만들 수 있는 모바일 서비스

더 이상 치킨 기프티콘, 디퓨저는 그만! 이제 진짜 갖고 싶은 선물을 받아보세요.

🔗 **서비스 링크**: [wishmoa.vercel.app](https://wishmoa.vercel.app)

---

## 📋 프로젝트 개요

- **다중 사용자** 플랫폼: 누구나 로그인하여 자신만의 펀딩 프로젝트 운영
- 프론트엔드 + 백엔드 + 관계형 데이터베이스
- **Vercel** 배포

---

## ✨ 주요 기능

### 🎁 선물 펀딩
- 원하는 선물을 등록하고 친구들에게 펀딩받기
- 여러 친구가 조금씩 모아 큰 선물 완성
- 진행률 시각화 및 후원자 아이콘 표시

### 🤖 AI 선물 추천 (BETA)
- **Google Gemini API** 기반 선물 추천
- 상황, 성별, 연령대, 가격대, 카테고리 선택
- 트렌디한 선물 아이템 추천

### 💸 토스 간편송금
- 토스 QR코드 업로드로 간편 송금 연동
- 후원 시 토스 앱으로 바로 이동
- 금액 자동 입력

### 🎨 테마 컬러
- 4가지 테마 선택: 🍇포도, 🍑복숭아, 🍊자몽, 🌱민트
- 프로젝트별 맞춤 디자인

### 🔒 보안
- 계좌번호 AES-256-GCM 암호화
- API Rate Limiting (악용 방지)
- 소유권 기반 접근 제어

---

## 🛠 기술 스택

| 구분 | 기술 / 라이브러리 | 역할 |
|------|------------------|------|
| **프레임워크** | Next.js 16 (App Router) | Vercel 최적화, SSR/SSG |
| **언어** | TypeScript | 타입 안정성 |
| **스타일링** | Tailwind CSS | 빠른 UI 개발, 커스텀 폰트 |
| **UI 컴포넌트** | shadcn/ui | Tailwind 기반 고품질 UI |
| **Drag & Drop** | dnd-kit | 선물 순서 변경 |
| **백엔드 API** | Next.js API Routes | 서버리스 API |
| **데이터베이스** | Neon (PostgreSQL) | 관계형 DB |
| **ORM** | Prisma | 타입 안전한 DB 통신 |
| **인증** | NextAuth.js | 네이버, 구글 소셜 로그인 |
| **파일 저장소** | Vercel Blob | 이미지 업로드 |
| **AI** | Google Gemini API | 선물 추천 |
| **QR 디코딩** | jsQR | 토스 QR코드 해석 |
| **배포** | Vercel | CI/CD |

---

## 📱 페이지 구성

### 1. 메인 페이지 (`/`)
- 서비스 소개 및 기능 안내
- 네이버 / Google 소셜 로그인
- "로그인 안하고 구경할래요" 데모 링크

### 2. 대시보드 (`/dashboard`)
- 내 펀딩 프로젝트 목록
- 새 펀딩 만들기
- 프로젝트 삭제 (편집 모드)

### 3. 새 펀딩 만들기 (`/dashboard/new`)
- 펀딩 타이틀 / 부제목 입력
- 계좌 정보 입력 (암호화 저장)
- 토스 QR코드 업로드 (선택)
- 후원 금액 설정 (최대 3개 + 자유 금액)
- 테마 컬러 선택

### 4. 펀딩 페이지 (`/p/[projectId]`) ⭐
> 비로그인 사용자도 접근 가능 (공개)

- 선물 목록 및 진행률 표시
- 후원자 아이콘 및 메시지 확인
- 후원 모달 (계좌 복사 / 토스 송금)
- 프로젝트 테마 컬러 반영

#### 소유자 전용 기능 (햄버거 메뉴)
- 선물 관리 페이지 이동
- 후원 관리 페이지 이동
- 프로젝트 설정 모달

### 5. 선물 관리 (`/p/[projectId]/items`)
> 프로젝트 소유자만 접근

- **✨ 선물 추천받기 (BETA)**: AI가 선물 추천
- 선물 추가 (제목, URL, 이미지, 목표금액)
- 드래그 앤 드롭 순서 변경
- 상태 관리: 활성 / 숨김 / 완료 / 삭제

### 6. 후원 관리 (`/p/[projectId]/donations`)
> 프로젝트 소유자만 접근

- 후원 목록 (최신순)
- 필터: 전체 / 대기 / 확인됨
- 입금 확인 처리
- 후원 삭제

---

## 📁 프로젝트 구조

```
giftfunding/
├── app/
│   ├── page.tsx                         # 메인 페이지 (로그인)
│   ├── layout.tsx                       # 루트 레이아웃
│   ├── globals.css                      # 전역 스타일
│   ├── dashboard/
│   │   ├── page.tsx                     # 대시보드
│   │   └── new/
│   │       └── page.tsx                 # 새 펀딩 만들기
│   ├── p/
│   │   └── [projectId]/
│   │       ├── page.tsx                 # 펀딩 페이지 (공개)
│   │       ├── items/
│   │       │   └── page.tsx             # 선물 관리
│   │       └── donations/
│   │           └── page.tsx             # 후원 관리
│   ├── api/
│   │   ├── auth/[...nextauth]/route.ts  # NextAuth.js
│   │   ├── projects/
│   │   │   ├── route.ts                 # 프로젝트 CRUD
│   │   │   └── [projectId]/
│   │   │       ├── route.ts             # 프로젝트 상세
│   │   │       └── donations/route.ts   # 프로젝트 후원 목록
│   │   ├── items/
│   │   │   ├── route.ts                 # 선물 생성
│   │   │   ├── [itemId]/route.ts        # 선물 수정/삭제
│   │   │   └── reorder/route.ts         # 순서 변경
│   │   ├── donations/
│   │   │   ├── route.ts                 # 후원 생성
│   │   │   └── [donationId]/route.ts    # 후원 수정/삭제
│   │   ├── gift-recommend/route.ts      # AI 선물 추천
│   │   └── upload/route.ts              # 이미지 업로드
│   └── image/                           # 정적 이미지
├── components/
│   ├── DonationModal.tsx                # 후원 모달
│   ├── ItemCard.tsx                     # 선물 카드
│   ├── GiftRecommendModal.tsx           # 선물 추천 모달
│   ├── TossQrGuideModal.tsx             # 토스 QR 가이드
│   ├── Providers.tsx                    # Context Providers
│   └── ui/                              # shadcn/ui 컴포넌트
├── lib/
│   ├── auth.ts                          # NextAuth 설정
│   ├── prisma.ts                        # Prisma 클라이언트
│   ├── constants.ts                     # 상수 정의
│   ├── utils.ts                         # 유틸리티 함수
│   ├── crypto.ts                        # 암호화/복호화
│   ├── qr-decoder.ts                    # QR코드 디코딩
│   └── rate-limit.ts                    # Rate Limiting
├── types/
│   ├── index.ts                         # 공통 타입
│   └── next-auth.d.ts                   # NextAuth 타입 확장
├── prisma/
│   └── schema.prisma                    # DB 스키마
└── public/
    └── font/                            # 커스텀 폰트
```

---

## 🗄️ 데이터베이스 스키마

### users (사용자)
| 필드 | 타입 | 설명 |
|------|------|------|
| id | UUID (PK) | 사용자 고유 ID |
| name | string | 사용자 이름 |
| email | string? | 이메일 (선택) |
| image | string? | 프로필 이미지 |

### projects (펀딩 프로젝트)
| 필드 | 타입 | 설명 |
|------|------|------|
| project_id | string (PK) | 짧은 ID (8자) |
| user_id | UUID (FK) | 소유자 ID |
| project_title | string | 제목 |
| project_subtitle | string | 부제목 |
| account_bank | string | 은행명 |
| account_number | string | 계좌번호 (암호화) |
| account_holder | string | 예금주 |
| toss_qr_link | string? | 토스 QR 링크 |
| donation_amounts | string? | 후원 금액 옵션 |
| theme_color | string | 테마 컬러 |
| project_status | enum | active/hidden/deleted |

### items (선물)
| 필드 | 타입 | 설명 |
|------|------|------|
| item_id | UUID (PK) | 선물 ID |
| project_id | string (FK) | 프로젝트 ID |
| item_title | string | 선물명 |
| item_url | string | 상품 링크 |
| item_image | string? | 이미지 URL |
| item_price | integer | 목표 금액 |
| item_status | enum | active/hidden/completed/deleted |
| item_order | integer | 표시 순서 |

### donations (후원)
| 필드 | 타입 | 설명 |
|------|------|------|
| donation_id | UUID (PK) | 후원 ID |
| item_id | UUID (FK) | 선물 ID |
| donator_nm | string | 후원자명 |
| donator_message | string? | 메시지 |
| donation_amount | integer | 금액 |
| donation_status | enum | pending/confirmed/deleted |

---

## 🚀 시작하기

### 1. 저장소 클론
```bash
git clone https://github.com/hintyu/giftfunding_wishmoa.git
cd giftfunding_wishmoa
```

### 2. 의존성 설치
```bash
npm install
```

### 3. 환경변수 설정
`.env.local` 파일 생성:
```env
# Database (Neon)
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"

# 계좌번호 암호화 키 (선택, 없으면 NEXTAUTH_SECRET 사용)
ENCRYPTION_KEY="your-encryption-key"

# Naver OAuth
NAVER_CLIENT_ID="xxx"
NAVER_CLIENT_SECRET="xxx"

# Google OAuth
GOOGLE_CLIENT_ID="xxx"
GOOGLE_CLIENT_SECRET="xxx"

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"

# Gemini API (선물 추천)
GEMINI_API_KEY="your-gemini-api-key"
```

### 4. 데이터베이스 설정
```bash
npx prisma migrate dev
npx prisma generate
```

### 5. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

---

## 📝 스크립트

| 명령어 | 설명 |
|--------|------|
| `npm run dev` | 개발 서버 실행 |
| `npm run build` | 프로덕션 빌드 |
| `npm run start` | 프로덕션 서버 실행 |
| `npm run lint` | ESLint 실행 |
| `npx prisma studio` | DB GUI 실행 |
| `npx prisma migrate dev` | DB 마이그레이션 |

---

## 🔐 보안 기능

- **계좌번호 암호화**: AES-256-GCM
- **Rate Limiting**: Gemini API 1분당 10회 제한
- **소유권 검증**: 모든 관리 API에서 세션 + 소유권 확인
- **Soft Delete**: 데이터 삭제 시 상태만 변경

---

## 🎨 디자인 시스템

### 테마 컬러
| 테마 | 이름 | Primary |
|------|------|---------|
| 🍇 | 포도 | `#381DFC` |
| 🍑 | 복숭아 | `#FF6B9D` |
| 🍊 | 자몽 | `#F97316` |
| 🌱 | 민트 | `#2ECC71` |

### 폰트
- **Primary**: Omyu Pretty (커스텀)
- **Fallback**: system-ui

### 특징
- 모바일 퍼스트 디자인
- 그라데이션 헤더
- 컬러풀한 후원자 아이콘
- 부드러운 애니메이션

---

## 📄 라이센스

이 프로젝트는 개인 프로젝트입니다.

---

Made with 💙 by hintyu
